<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @talk.content %>
</p>

<p>
  <strong>Voice:</strong>
  <%= @talk.voice_jp %>
</p>
<p>
  <audio
    id="talk"
    controls
    src="<%= '/'+File.basename(@talk.audio_path) %>">
    Your browser does not support the <code>audio</code> element.
  </audio>
</p>

<%= link_to 'Back', talks_path %>
